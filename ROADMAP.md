# DailyWins Roadmap

Last updated: June 12, 2026

## Status
Phases **4 (audit log), 5 (act-as), and 6 (Site Admin schedule editor)** are shipped and live-verified. The "vendor cannot see student data, by database design" story is structurally true — RLS at the DB layer, audit triggers on PII + admin/config tables, PII-blind Operator/District-Admin tiers — and is now documented on `/privacy`. **Email is live (Resend)** and **magic links now work cross-device, both templates**. As of 6/08 the **Sure Step Education design system** is live across landing + dashboard (Fraunces / Public Sans / IBM Plex Mono, paper/ink palette, green accent, status scale; "Classic DailyWins" theme preserved), and the **all-perspectives** work shipped: a founder on-ramp to grant site_admin / district_admin roles (migration 033), a pure-admin landing fix, and student + co-teacher magic-link views — so all six vantage points (founder, teacher, site admin, district admin, parent, student) are now walkable. The **v1.1 admin-tier build-out** also shipped 6/08: role-aware landings (site admin → bell-schedule uploader, now gated by `role_assignments` not the legacy `school_admins`; district admin → PII-blind usage dashboard `/admin/usage`), the **Site Admin tier made truly PII-blind** (RLS fix 035 — removed an errant site_admin read grant on scores/notes/roster), **email-bound teacher invites** sent in-app via Resend (037, replacing the broken URL-token flow), **deactivate/reactivate teachers**, and a **session-refresh proxy + `/auth/home`** that fixed an admin login loop. Pilot is approval-scoped. **Later on 6/08** three more shipped: **sign-out on every admin surface** (district admins previously had no nav/logout at all), **parent/student/co-teacher magic-link behavior charts** (daily/weekly/monthly bars — overall %-color-graded + per-category breakdown using the teacher's own labels — replacing the meaningless cumulative period numbers; migration 038), and a fix to **pin the teacher's schedule to their assigned school** (the Bell Schedule modal's hardcoded two-school picker is gone — school comes from `school_id`, read-only). A **role-hierarchy test cluster** (surestep2 district → devintest2 site admin → devintest3 teacher, all @ South Sac HS / Sacramento district) was stood up, which **walked the email-bound teacher invite end-to-end on prod — it works.** **On 6/09** a big follow-on session shipped: **co-teacher readwrite links can now actually write scores/notes** (migration 039, the long-open write UI — backend existed since 021), an **arrival-charting bug fix** (arrival stores an option index, not points — charts were under-reporting it ~3% vs true ~92%), **progress-icon continuity** (3 icons; the teacher's icon now badges the magic-link views + PDFs; migration 040), **B&W-safe bar charts in the Daily/Weekly PDFs** (in-cell mini bars), a **horizontal per-period quick-fill** (click the period title), and a Customize/dashboard cleanup pass (6 color themes incl. a pink Rose, Inter-only font, removed Compact Mode / Parent View / School Team / Student Sync / Export to Drive; support@ email). **Prod migration head: `040`.** **Late 6/09–6/10:** the **Sure Step company aesthetic** went live app-wide (DM Serif Display/DM Sans/DM Mono, navy/forest/teal/amber tokens) with a **unified 2.5s splash** (aesthetic MD §5); an **NPS marketing campaign** was built outside the repo (`Desktop/.../dailywins-marketing/` — website + deck + pricing proposal + demo script + director emails) with the marketing site **live at dailywins-schools.vercel.app** (separate Vercel project; `dw.surestepeducation.com` pending a Cloudflare CNAME); and the repo gained **ARCHITECTURE.md** (front-end/back-end map) plus persistent **`frontend`/`backend` working branches** (nine stale merged branches pruned). **6/10 pm — the NPS go-to-market day, migrations 041→047:** site-admin link oversight + audited revoke (041), scoped audit-read for district/site admins, splash once-per-session + QuietLoader, editable zone names + quick-fill level + "Co-teacher / Paraprofessional" rename, founder per-district usage (042), the reason-gated audited **district notes archive** (043) with /privacy rewritten so **the district plainly owns its full record** (PII-blindness now claimed only for the Operator), **student self-assessment** via readwrite student links (044, separate `self_assessments` table; teacher sees self-checks in Entry view), single-email invites with **branded `/welcome/<code>` short links** (045) + OTP expiry 1h→24h + the **approval email** that closes the signup loop, **`org_type` one-app/two-shapes** + one-step "NPS Director" approval (046), and the **NPS director login** (047): full school records at `/admin/records` (all points + all notes incl. private, audited per open) + per-type **link policy** enforced inside `generate_magic_link`. Business: **pricing decided** ($199/mo flat, unlimited teachers; founding $149/mo for first 15 CA schools — live on the marketing site), Devin has a **260-school CA NPS list**, and **FIELD-GUIDE.md** documents the estate + service-call protocol. **6/11 — director-experience hardening:** the first NPS org (**NBPS**, surestep3 as pseudo-director) exists on prod and walked the real flow; everything it hit got fixed: **/admin/home** director home + **gamified one-step launch wizard** on the navy stage; **Resend sign-in** rescue buttons + landing-page "this is also how you sign back in" copy; the **schedule parse went async** (migration **048** `schedule_parse_jobs` job+poll — survives VPNs/proxies; streamed Anthropic call at 32k tokens; the 17-variant Monterey Trail monster parses reliably); review now celebrates and asks **one quick check at a time**, variant names auto-dedupe, and **save failures return to the review screen** (never discard a reviewed parse); emails teach bookmarking dailywins.school; audit log untrapped for NPS directors + actor emails fixed (PostgREST hides the auth schema); the whole admin shell unified behind one **AdminNavyBand** (same slim navy band, same spot, all seven tabs, one shared page frame); **"What teachers see"** demo dashboard (seeds the director's own teacher dashboard with [DEMO] students; `preferences.admin_first` keeps their sign-in landing on /admin/home); and **two-agent worktrees** (`dailywins-frontend`/`dailywins-backend` folders on the synced `frontend`/`backend` branches — quickstart in ARCHITECTURE.md; stale claude worktree pruned). Prod migration head: **048**. **6/11 pm–6/12 — hardening + compliance + The Locker (migrations 049→055):** the director-console **hardening sweep** (student soft-delete 049, per-teacher category scoring 050 + Arrival-0% fix, usage hygiene 051, launch-finished flag 052, rebuilt branded Print PDF with audited `nps_record.print` + date range, audit-log visibility fix, org-aware privacy copy); the **CSDPA vendor packet** drafted (docs/compliance/ + PDFs on Devin's Desktop + EGUSD outreach email); and **The Locker** shipped end-to-end at `/locker` (053 spine: canonical_id, combo-slip identities + device-claim cookie, append-only points_ledger with DB-enforced immutability, inventory, JSONB layouts; 054 cap 40; 055 teacher shelf): whole-open-locker canvas with drag/layer/rotate/resize, slow-swing door ritual (ONE opening — the combo page's fake door-open was cut), Store/Bank/Shoebox as centered modals, **seven original sticker packs** (98 catalog items, "evoke the genre never the franchise"), functional cards (Goal w/ target picker, ∞ My Best Work Doc/Slides showcase w/ server-enforced URL allowlist, tap-to-check Month card; Today card built then retired), **teacher reward shelf** (hw passes → use → confirm, audited, ledger-untouched), lazy daily earn at a per-class rate, optimistic-concurrency + chained saves (two real multi-tab/race bugs found and fixed live). Prod migration head: **055**.

## Director-console hardening punch list (6/11 — Claude red-team + external console review)

**Critical (ship before school #1):**
- ~~Soft-delete students~~ — ✅ shipped 6/11 (049): teacher hard-DELETE policy dropped; teachers archive (`archived_at`, reversible, audited `student.archive`); director roster shows archived students badged "record retained"; teacher dashboards filter them out. Hard delete = service-role only.
- ~~Multi-teacher category collision~~ — ✅ shipped 6/11 (050): `nps_get_student_record` returns `teacher_id` per score row + `categories_by_teacher`; charts + PDF score every row against ITS teacher's config (verified with a two-config rendered PDF). **Still open: the 038 magic-link views** share the old dominant-teacher convention — smaller blast radius (links are teacher-generated) but queued for the same treatment.
- ~~Record opens invisible in the director's audit log~~ — ✅ fixed 6/11: `nps_record.*` rows (id-only, no PII payload) now pass the scoped-audit PII filter; audit + links footers rewritten per org shape (NPS director sees the truth, district admins keep the PII-blind copy).
- **DPA / CSDPA registry story** — playbook written: **[docs/CSDPA-REGISTRATION.md](docs/CSDPA-REGISTRATION.md)** (no agency — sign the NDPA+CA exhibits with one originating LEA incl. Exhibit E General Offer, get posted to the SDPC registry; EGUSD is the natural originator). ✅ Vendor packet DRAFTED 6/12 (docs/compliance/: data inventory, subprocessors, security summary, breach notification, Exhibit B worksheet + EGUSD outreach email; PDFs rendered to Devin's Desktop/compliance/). Devin's remaining moves: counsel pass on /privacy, send the EGUSD email.

**High (trust + compliance):**
- ~~Private-note disclosure to teachers~~ — ✅ shipped 6/11: disclosure line under the note composer toggle + a paragraph in the teacher invite email ("private" = hidden from parents/students/other teachers; the official record and records-access admins still include it).
- ~~Audit + watermark PDF generation~~ — ✅ shipped 6/11: every Print PDF writes `nps_record.print` to the audit log (allowlisted in /api/audit/event with an is_nps_school_admin re-check); footer stamps "generated {date} by {email} · record access and exports are audited".
- **Record/PDF compliance fields** — ✅ date-range picker shipped 6/11 (record view filters charts+notes; PDF shows EVERY bucket in an explicit window + prints the window in the header). **Still open:** student ID / placing-district fields on the roster; attendance summary (NPS billing is ADA-based).
- **Second school admin** — director can't mint an assistant/successor; one human + one inbox is the school's single point of failure (NPS director turnover is real). Related: shared office@ inboxes → revisit deferred OTP-code/Microsoft-SSO ideas.

**Medium (self-inflicted, quick):**
- ~~Demo-data containment~~ — ✅ shipped 6/11: "Wipe demo data" button on the home card (POST {action:"wipe"}, audited `demo_dashboard.wipe`) + card copy warns to wipe before inviting real staff.
- ~~Director-as-teacher pollution~~ — ✅ shipped 6/11 (051 + app): `admin_first` rows excluded from get_site_usage totals + teacher list, the Teachers tab roster, and home counts; [DEMO] + archived students excluded from student counts.
- ~~Launch `finished` flag per-device~~ — ✅ shipped 6/11 (052): `schools.launch_finished_at` set via /api/admin/launch-finished when the wizard completes; home passes it down so any device lands on mission control.
- **September lockout wave** — sessions die over summer; plan a "welcome back" email with a sign-in button in August (calendar item, not code).
- ~~Copy/counter consistency pass~~ — ✅ shipped 6/11: home schedule tile uses the Bell-schedules vocabulary ("N variants"); the staff blurb only promises parent links when the policy has them on.

**External-review items triaged out:** surestep3@proton.me visibility = test-account artifact, not product (real directors see their own email); teacher invites "no approval" is by design (the DIRECTOR is the approval; links are already single-use/24h); the reported deep-link bounce on /admin/upload-schedule has no code path that explains it — needs a repro before chasing.

**Business plumbing (decide, not code):** billing (Stripe at >15 schools?), export/offboarding ("download everything" + retention promise), Supabase PITR + documented recovery promise, mobile eyeball of admin tables at phone width. Backlog niceties: audit search/filter/export + per-student "who viewed" timeline, empty-state CTAs, link-toggle confirmation with affected-link count, "what parents see" preview per link type.

## Open — sorted by what blocks the NPS launch (this month) or EGUSD (July 13)
- **NPS end-to-end walk** — fresh signup → approve as NPS Director → approval email → /welcome → director dashboard → Student records → Link policy → invite teacher → links respect policy. The exact first-customer flow; nothing else matters until it's eyeballed.
- **Marketing launch steps** — Cloudflare CNAME for dw.surestepeducation.com; demo dry-run with Nick; first 25-school email batch (260-school CA list in hand; artifacts in `dailywins-marketing/`).
- **Counsel pass on /privacy §7** — district record ownership + NPS director records access wording (342dcf1, ad0c0bb).
- **Demo script for EGUSD** — Devin is building it in a separate project. The compliance walkthrough: approve-under-audit → act-as picker → coral banner → audit-log artifact → break-glass as the strongest visual proof. Claude can draft a DW clickpath on request.
- ~~Demo Mode reseed.~~ **Verified healthy 6/02.** Live demo data already reflects the `1f6ba4a` fix (phoneAway non-zero in 1366/1590 rows, onTask in 1586/1590 — correct distributions, not the old all-zero bug) and is only ~4 days stale (Apr 6 → May 29). No reseed needed now; reseed for date-freshness right before a dry-run / the demo via **Demo Mode → Wipe → Seed** in the dashboard (or ask Claude to script it). Worth a quick in-app glance during a dry-run to confirm the dashboard *renders* it right.
- **General audit gap for direct admin/MCP SQL** — the trigger no-ops on service-role context (auth.uid() NULL). 032 + the 6/04 schedule-audit attribution closed this for *schedule* edits app-side (`writeAuditLog('schedule.update')` now stamps actor + acting-as); the general case (arbitrary admin SQL) still wants a session-variable "intended actor" so the trigger can attribute to a real user.
- **Drop `allowed_emails`** — vestigial since the auth-callback rewrite stopped reading it. Drop after a quiet observation period (snapshot first — it's a one-way door).

## Follow-ups from recent work
- **Walk the deployed-prod tier verifications** — invite round-trip WALKED; user confirmed the new single-email invite loop + self-assess work (6/10). Still unwalked: **deactivate/reactivate**, PDF B&W bars eyeball, arrival-fix eyeball.
- ~~Lunch-pref hardcode~~ — ✅ removed 6/10 (`2105d09`); lunch toggle is schedule-driven.
- ~~Raise magic-link OTP expiry to 24h~~ — ✅ done 6/10 via Management API (3600→86400).
- ~~Site-admin magic-link revocation UI~~ — ✅ shipped 6/10 (041, `46475f2`): /admin/links list + audited revoke; superseded further by the 047 link policy.
- ~~**Co-teacher write UI**~~ — ✅ shipped 6/09 (039); user-confirmed working 6/10.
- **PDF bar charts + arrival-charting fix (6/09)** — both shipped but NOT visually confirmed on deployed prod: download a Daily/Weekly PDF (check the B&W mini-bars), and confirm Arrival now reads ~90s% instead of near-0 in the charts.
- **Single-typeface / Rose polish (optional)** — body font is now Inter-only but headings still use Fraunces (`DISPLAY_FONT`); Rose theme still has a green secondary. Both are quick if a fuller commitment is wanted.
- ~~Scoped audit-read~~ — ✅ shipped 6/10 (`90fd5df`): district/site admins read their domain, PII-table rows excluded; RLS stays founder-only as the DB backstop.
- **Single-email invite (optional)** — magic-link sign-in sends two emails (our invite + Supabase's link); a server-generated sign-in link would collapse it to one. Google path is already one click.
- **Re-evaluate the `school_schedules` table (~10h)** — was slated to move bell schedules out of hardcoded TS. The JSONB-on-`schools` column + the editor already solved the practical problem, so decide whether a normalized table still earns its cost before building it.
- **Live-verify the act-as schedule items** — build/typecheck pass, but founder-implicit edit (#2), act-as audit attribution (#3), and sliding expiry (#4) only fully exercise under an active act-as session. Walk one in prod: start act-as → edit a schedule → confirm the audit row carries `acting_as_user_id` and `expires_at` slides on activity. Optional: extend the act-as attribution lookup / trigger coverage to the `teachers` table.

## EGUSD July 13 prep
- ✅ Compliance story demo-able end-to-end: `/admin/requests` (approve under audit) → `/admin/teachers` (act-as) → coral banner → `/admin/audit-log`.
- ✅ `/privacy` reflects the shipped architecture (RLS, four-tier PII-blindness, audited act-as, magic-link, parent read-only link, Anthropic subprocessor). **Pending:** confirm exact data-residency region (de-specified "Ohio"→"US East"; prod is us-east-1), counsel eyeball on the PII-blindness + Anthropic "not used to train" wording.
- Demo script — not started (see Open).
- Mock meeting with Nick — not scheduled.
- Compliance *folder* (DPA templates: CSDPA / National DPA) — TODO; `/privacy` says we're ready to sign one.

## The Locker — shipped 6/12, carried items
Shipped (see Status ¶ for the full list): spine 053–055, canvas + door
ritual, 7 packs / 98 items, Goal/Best-Work/Month cards, teacher shelf,
economy, concurrency fixes. Docs live in `docs/locker/` (spec, data model,
economy, decisions log, sticker-pack self-audit, functional objects).
**Carried:** Song of the Week (explicit-filter only), countdown card,
progress printout, sticky notes (AFTER the school visibility toggle), the
school visibility toggle itself (decision #3), identity-merge tooling
(canonical_id exists, no admin UI), proud-work allowlist extension for
district portfolio tools, Chromebook perf test at the 40-item cap, and a
look at the welcome-grant-vs-store-size ratio (full collection ≈ 2,500 pts;
earn rate 0.5–2× is the lever).

## Recently shipped (newest first)
- **6/11 pm–6/12 — Hardening + compliance + The Locker (migrations 049→055; `3361278`→`98a392c`).** Punch-list criticals/highs/mediums knocked out (see ✅ marks above); CSDPA vendor packet drafted + PDF'd; The Locker v1 from zero → seven packs, four-then-three functional cards, teacher shelf, wallet economy, single door-opening ritual, two live-found concurrency bugs fixed (stale-tab clobber → optimistic concurrency; rapid-tap save race → chained saves). Locker activated for NBPS (surestep3) and founder@PGHS; [DEMO] claims get a pre-decorated showcase.
- **6/11 — Director-experience day (migration 048; ~18 commits `259097e`→`3fe1bc5`).** Director home `/admin/home` + gamified launch wizard; Resend-sign-in rescue; async schedule parse (job+poll, 048) proven on the 17-variant Monterey Trail PDF; one-at-a-time review checks + dedupe + never-lose-the-review; bookmark-teaching emails; audit-log fixes for NPS directors; AdminNavyBand shell unification across all admin tabs; "What teachers see" demo dashboard (`admin_first` routing flag); two-agent worktrees (`dailywins-frontend`/`dailywins-backend`). First NPS org **NBPS** live on prod with the schedule on file. Pending eyeball: monster-schedule retest, demo-dashboard walk, NBPS teacher-invite + link-policy check.
- **6/10 — Front-end/back-end organization (`5f51b53`).** `ARCHITECTURE.md` maps every source file to FE vs BE, documents the three FE↔BE seams (browser RPCs, internal API routes, RLS reads), and defines the branch workflow. Persistent `frontend` + `backend` branches pushed to origin; nine stale merged local branches pruned. Repo NOT physically split (App Router needs one tree).
- **6/10 — NPS marketing campaign + live marketing site (outside repo).** `dailywins-marketing/`: standalone website (interactive tap-to-win demo grid), 13-slide HTML deck, cost-structure proposal (**$149/mo School + free 60-day pilot — needs Devin/Nick sign-off**), 15-min demo script (Demo-Mode-only), 4-email director sequence. Deployed to **dailywins-schools.vercel.app** as a separate Vercel project; `dw.surestepeducation.com` awaits a Cloudflare CNAME (`dw` → `cname.vercel-dns.com`, DNS-only). surestepeducation.com itself is Netlify-hosted — don't redeploy the stale local copy for a `/dw` path.
- **6/09 pm — Company aesthetic + unified splash (`f36fe24`, `55cce1f`, `8c57fd7`).** `Sure_Step_Education_Aesthetic.md` applied: DM trio via next/font, `--ssd-*` tokens → company palette, dashboard COLORS/default theme repointed. Single `Splash` component for all three loading moments; `SplashGate` holds it 2.5s then dissolves so it can't flash. ⚠️ Eyeball on prod pending; error boundary still coral.
- **6/09 — PDF reports use B&W-safe in-cell bar charts (`e45ab31`).** Daily + Weekly PDFs draw each score cell as a mini bar whose LENGTH encodes the value + the printed number (reads in color/grayscale/pure B&W). autoTable `didDrawCell` + `drawCellBar()`. Browser-only output → needs a visual eyeball.
- **6/09 — Progress-icon continuity (`ce434aa`, migration 040).** Picker narrowed to ⭐/🏆/🎯; the teacher's icon now badges the parent/student/co-teacher magic-link view (before the student name) + the Daily/Weekly PDFs (canvas→PNG, since jsPDF can't draw emoji text). Migration 040 returns `progress_icon` from the three view RPCs (default ⭐).
- **6/09 — Customize/dashboard cleanup.** Per-period horizontal quick-fill via the period title (`quickFillPeriod`), removed the per-category "All → X" buttons; "Feedback to Devin" → `mailto:support@surestepeducation.com` (`c46cd69`). Removed stale Parent View / School Team / Student Sync / Export to Drive (`e15c563`, `878db2f`). Color themes 9 → 6 incl. a pink Rose (`eb64cc0`); Inter-only font (`6b26288`); Compact Mode removed (`206a0d1`).
- **6/09 — Arrival under-reported in charts, fixed (`b0ea814`).** Arrival stores an option INDEX (pointValues collide, e.g. [3,0,3]); both `extractScores` paths summed it as points, so "On Time" charted as 0 not 3. Added `pointsForRaw` in ChartViews + BehaviorCharts. Dashboard headline numbers were already correct. Logic-verified; not yet eyeballed live.
- **6/09 — Co-teacher readwrite links can write (`9ba8d1f`, migration 039).** New `CoteacherWritePanel` (readwrite only): add-today's-scores form + shared-note composer, calling `coteacher_write_score`/`_note` via the anon client (token-validated, attributed to the lead teacher). Migration 039 makes the score RPC an UPSERT that merges per-key into today's row. Banner reworded. Snapshot `.snapshots/039-pre-coteacher-upsert.sql`.
- **6/08 pm — Teacher schedule pinned to the assigned school (`fa481c5`).** The Bell Schedule modal had a hardcoded two-school picker (`SCHOOLS = [Cosumnes Oaks, Pleasant Grove]`) in localStorage, decoupled from `school_id` — every teacher saw both pilot schools; a teacher elsewhere (e.g. South Sac HS) saw two that weren't theirs. Now the school comes from `profile.school_name` (the site-admin-set assignment), shown read-only; the teacher keeps only schedule-variant + 1st/2nd lunch. Site-admin check keys off `teacher.school_id`; empty states for "no school yet" / "no schedule set yet"; dead `SCHOOLS`/`SCHOOL_NAME_TO_ID`/`BELL_SCHEDULES`/`SchoolName` removed.
- **6/08 pm — Parent/student/co-teacher magic-link behavior charts (`c839207`, migration 038).** Replaced the one cumulative number per period with a Daily/Weekly/Monthly toggle → (1) overall %-of-goals-met bar chart, color-graded green/gold/coral, and (2) a per-category breakdown labeled with the teacher's own category names (custom labels + max points flow from `teachers.categories`); notes kept. Migration 038 extends `get_parent_view`/`get_student_view`/`get_coteacher_view` to return `score_date` + legacy per-cat columns + the scoring teacher's `categories` (additive; private-note rule + scope guards preserved). Recharts behind `dynamic(ssr:false)`; per-cat bars plain CSS. Verified locally w/ synthetic data. Pre-038 snapshot at `.snapshots/038-pre-magic-link-views.sql`.
- **6/08 pm — Sign-out on every admin surface (`9084c1a`).** Admin pages had no logout (only the teacher dashboard did); district admins had no nav at all on `/admin/usage`. Reusable `SignOutButton` (session-clear → hard redirect to `/`) added top-right on `/admin/usage` (both branches + error/empty), `/admin/upload-schedule`, `/admin/teachers` (site admin + founder).
- **6/08 — Email-bound teacher invites, in-app (037, `f292309`).** The URL-token invite was broken (the magic-link email template drops `?invite=`, so invited teachers fell to a pending signup). Rebuilt: admin types the teacher's email on `/admin/teachers` → `create_teacher_invite` binds it + Resend emails the teacher (`send-teacher-invite.ts`) → on sign-in `claim_email_teacher_invite` matches the verified email and provisions teacher → `/dashboard`. `/?email=` prefills the landing page; old `?invite=` plumbing removed. Cleaned the stray devintest3 pending request + dead invite. **End-to-end deployed-prod walk still pending.**
- **6/08 — Auth-loop fix: SSR session proxy + role-aware home (`b5b202d`).** There was no session-refresh middleware (server admin pages lost the session after ~1h); added `proxy.ts` (Next 16's renamed middleware). The landing page sent every authed user to `/dashboard`, looping admin accounts via `/pending`; added `/auth/home` resolver. Magic-link failures were mostly **expired 1h OTPs** — recommend raising OTP expiry to 24h in Supabase Auth.
- **6/08 — Site admin invite + deactivate teachers (036) + nav (`6ec92d6`, `4a15ea6`).** `teachers.deactivated_at` + `set_teacher_active` (blocks login, reversible, audited). `SiteAdminNav` cross-links the three site-admin surfaces. (Invite later superseded by the email-bound 037 redesign.)
- **6/08 — Site Admin tier made PII-blind (035, `cd16574`).** Removed the `has_role('site_admin', …)` clause from `scores_role_read` / `notes_role_read` / `students_role_read` — a site admin could read student scores/shared-notes/roster, contradicting `/privacy`. Teacher access untouched; verified under RLS (site admin → 0 rows; teacher → 12 students + 1680 scores). District admin + founder were already blind.
- **6/08 — Tier dashboards + role-aware landings (034, `1d48202`).** Site admin home = bell-schedule uploader (gating migrated `school_admins` → `role_assignments`, page + save route); district admin home = PII-blind `/admin/usage` (per-school rollups + schedule coverage) via SECURITY DEFINER aggregate RPCs `get_district_usage` / `get_site_usage` (counts only). District admins no longer see a teacher roster (`/admin/teachers` is founder + site_admin).
- **6/08 — All perspectives walkable: admin on-ramp + student/co-teacher links (`fe3c2a9`).** migration 033 `approve_access_request_as_role` lets a founder provision an approved request as teacher / site_admin / district_admin with the right scope; `/api/admin/approve` + approval modal gained a role+scope picker; new `/api/admin/districts`. Pure admins (role, no teachers row) now land on `/admin/teachers` instead of bouncing to `/pending`. New `/student/[token]` + `/coteacher/[token]` pages (shared `MagicLinkSummary`; parent refactored onto it); `ManageLinksModal` generates parent/student/co-teacher links. Build + typecheck green; migration applied to prod (user-authorized). Test-account walk on deployed prod still pending (Devin's +alias step).
- **6/08 — Sure Step Education design system (`966160b`).** Fraunces/Public Sans/IBM Plex Mono via next/font; full token set in Tailwind v4 `@theme`; landing + dashboard reskinned by repointing the `COLORS` constants + `default` theme to design tokens (standing zones now use the §2 status scale). "Classic DailyWins" theme preserved; Public Sans + Fraunces added to the font switcher.
- **6/04 — Phase 4/5/6 follow-ups (`659a73a`).** (1) Founder-implicit schedule edit: a founder manages any school's bell schedule without a `school_admins` row — `/admin/upload-schedule` loads all schools for founders, the save route falls back to `has_role('founder')`; both resolve via `effective_user_id()` so act-as scopes to the target. (2) Act-as attribution: `schedule.update` audit now stamps `acting_as_user_id` + `break_glass` via `getCurrentActAsSession()`. (3) Inactivity-based act-as expiry: regular sessions slide `expires_at` forward on activity (1 write / 5 min throttle); break-glass stays hard-capped. Build + typecheck green; live-verification under act-as still pending (see Follow-ups).
- **6/04 — "Magic Link" email template flipped (Devin).** The last open auth item; existing-user cross-device re-request links now work. Magic-link saga fully closed.
- **6/04 — Test-account cleanup.** Deleted ~9 `devintest*@proton.me` accounts (9 `auth.users` + 3 `access_requests`; one test teacher/role cascaded, 0 behavior rows). Snapshot at `.snapshots/2026-06-04-…json` (gitignored). 6 real users / 3 founders intact.
- **6/03 — Email live (Resend) + founder notifications.** Domain `send.dailywins.school` verified (DNS at GoDaddy: DKIM/SPF/MX/DMARC), `RESEND_API_KEY` + `NOTIFY_FROM_EMAIL` + `NOTIFY_TO_EMAIL` set in Vercel, Supabase custom SMTP wired (smtp.resend.com:465) so magic-link delivery is fast/reliable. `NOTIFY_TO_EMAIL` → `devinfarren@gmail.com` (surestep mailbox isn't bridged to Gmail). All verified in Resend logs. New domain → first sends land in Spam/Promotions until reputation warms (mark "Not spam").
- **6/03 — Cross-device magic links fixed (`73aef2c`).** Magic-link sign-in used the PKCE code flow (`exchangeCodeForSession` needs the requesting browser's `code_verifier` cookie) → clicking on a different device/browser left `email_confirmed_at` set but `last_sign_in_at` null, no session, no access-request row. New `/auth/confirm` route verifies a `token_hash` via `verifyOtp` (no cookie) → works cross-device; also accepts a `?code=` fallback. Shared provisioning gate extracted to `src/lib/auth-provision.ts` (used by both `/auth/callback` OAuth and `/auth/confirm` email). Email templates flipped to the `token_hash` URL (Confirm-signup verified; Magic-Link pending — see Follow-ups). Proven in prod via an incognito (cross-context) test.
- **6/03 — `/pending` gated + dead route removed (`aca6bfc`).** `/pending` was an ungated client page (unauth visitors saw the shell; its `/api/access-request/mine` fetch 401'd → "Unable to load your request status"). Now a server component: `getUser()` → redirect (no session → `/`, approved → `/dashboard`, denied/missing → `/access-denied`, pending → render with status/school as props). Removed `/api/access-request/mine` (sole consumer was `/pending`). Verified in prod (unauth `/pending` → 307 `/`; mine → 404).
- **6/02 — Phase 4 audit expansion (migration 032).** Generic 029 trigger extended to `role_assignments`, `school_admins`, `schools`, `districts` (alias map only; 029 tables untouched) → 21 triggers. Schedule edits (service-role) audited app-side. Applied + verified in prod (structural + rolled-back behavioral test, zero residue).
- **6/02 — `/privacy` refresh.** Now matches shipped security architecture; claims verified against live RLS policies.
- **6/01 — Phase 6 Site Admin schedule editor.** DB-backed page (loads the user's own `school_admins` schools), edit-existing path via `dbShapeToExtracted()`, merge vs replace save modes, add/remove variants & periods. Live-verified on prod; remove-period confirm + last-period guard added after testing.
- **6/01 — Migration 031, legacy arrival cleanup.** Finished the 6 out-of-range `arrival=3` rows the 5/27 live SQL missed (→ index 0). Applied + verified. Also closed the 027 ledger-name-drift and "May 27 staging RLS" non-items.
- **5/31 — Tommy pilot status resolved** (intentionally dormant; self-provisions via `/admin/requests`).
- **5/30 — Phase 4 audit triggers (029), `teachers.preferences` (030), "Show Period 0", `BELL_SCHEDULES` purge, passwordless magic-link login.**
- **5/29 — Break-glass UI** (founder-only, rose-red banner + required reason, 15-min timeout). Verified live.
- **Earlier — Phase 5 act-as (027/028):** districts/act_as_sessions/audit_log, `effective_user_id()`, RLS rewrite, act-as + break-glass routes, `/admin/teachers`, `/admin/audit-log`, `/audit/me`. **Beta access layer (022–026):** access_requests + RLS, approve RPC, `/pending`, `/admin/requests`.

## Pilot status (snapshot 6/04)
- **Devin** (founder + teacher) — PGHS; admins PGHS + COHS.
- **Nick** (teacher + Site Admin) — COHS.
- 6 `auth.users` total / 3 teachers after the `devintest*` cleanup.
- Approval-gated onboarding is the gate; `allowed_emails` is vestigial.

## Working guardrails (current)
- **Reversibility gate** (adopted 6/01): do anything reversible — including backed-up prod data writes; **always snapshot before a prod mutation.** Queue genuine one-way doors (force-push/history rewrite, un-backed-up destructive SQL, bulk ops, real-user emails, infra teardown, auth-config lockout) for an attended, approved moment.
- **Guarded-apply or stage-test migrations** — capture the restore point first; verify after.
- A passing type-check is not a working feature — close the loop in the app.
- `auth.uid()` for attribution, `effective_user_id()` for data access — mixing them caused both 5/29 act-as regressions.
- `onConflict` upserts require a real UNIQUE constraint — verify before trusting.
- RPC responses are not interchangeable with raw row SELECTs even on the same data — watch for shape mismatches.

## Infrastructure
- **Prod:** Supabase `kvbpfvazddlmoxobqfev` (us-east-1). Vercel, one project, three domains. **Migration head: `048`.** Supabase MCP pinned to prod via an `sbp_…` PAT in `~/.claude.json` (no dev branches; staging is a separate, MCP-unreachable project).
- **Staging:** Supabase `oqhhpdaijscqdkpsxowq` (us-east-2). Pause manually to save the t4g.nano cost; restoring takes a few minutes.
- **Git:** `main` deploys; `frontend`/`backend` are long-lived working branches checked out as worktrees (`../dailywins-frontend`, `../dailywins-backend`) for parallel agent sessions — see ARCHITECTURE.md.
